= form_for @content_archive do |f|
  -if @content_archive.errors.any?
    #error_explanation
      %h2= "#{pluralize(@content_archive.errors.count, "error")} prohibited this content_archive from being saved:"
      %ul
        - @content_archive.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.cktext_area :content
  .field
    = f.check_box :clickable
    = "Check the box if you want to display the Title in the Menu as a Link"
  .actions
    = f.submit 'Save'
