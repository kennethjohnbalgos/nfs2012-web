= render partial: 'shared/content_header'
.content
  .right_space
    .announcement_title
      = image_tag('menu_title_sponsors.jpg')
    .announcement_list
      = render partial: 'menu'
  .left_space
    .title
      = @content_sponsor.title if @content_sponsor.present?
    .context
      =raw @content_sponsor.content if @content_sponsor.present?
= render partial: 'shared/footer'