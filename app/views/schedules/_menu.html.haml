- @content_schedules.each do |c|
  - if c.clickable?
    .announcement
      = link_to c.title, {controller: 'schedules', action: 'content', id: c.id}, remote: true
  - else
    .announcement_notlink
      = c.title