= form_for @content_about do |f|
  -if @content_about.errors.any?
    #error_explanation
      %h2= "#{pluralize(@content_about.errors.count, "error")} prohibited this content_about from being saved:"
      %ul
        - @content_about.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.cktext_area :content
  .field
    = f.label :position
    = f.text_field :position
  .field
    = f.check_box :clickable
    = "Check the box if you want to display the Title in the Menu as a Link"
  .actions
    = f.submit 'Save'
