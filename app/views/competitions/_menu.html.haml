- @content_competitions.each do |c|
  - if c.clickable?
    .announcement
      = link_to raw(c.title.gsub("_","&nbsp;")), {controller: 'competitions', action: 'content', id: c.id}, remote: true
  - else
    .announcement_notlink
      = c.title